# QuizRoulette — Functional Backlog

Player-facing behavior & content, grouped into three themes. The fourth theme, **Non-functional** (architecture, tooling, deploy, tech debt), lives in [BACKLOG_TECHNICAL.md](BACKLOG_TECHNICAL.md).

1. **Graphics & Sound** — how the game looks, moves and sounds.
2. **Game play** — the loop, modes, progression, playing with others.
3. **Curriculum** — what is asked, how much of it, and how it maps to school.
4. **Non-functional** — in the technical backlog.

> **Strategic note — v2 pivot.** The project is moving from a personal *static* toy to a *connected app*. Several items below depend on a backend + accounts (see Technical `T-03`, `T-05`). This retires the original "no backend / no build / offline `file://`" guardrails — a deliberate choice to be ratified in REQUIREMENTS.md.

**Legend**
- **Size (T-shirt):** `S` <2h · `M` ~half-day · `L` multi-day · `XL` multi-session, split before starting.
- **Priority:** `High` do next / foundational · `Med` important soon · `Low` later / vision.
- **Status:** `Todo` · `In progress` · `Blocked` · `Done`.

## 1. Graphics & Sound

| ID | Item — what & why | Size | Priority | Depends on | Status |
|----|-------------------|:----:|:--------:|-----------|:------:|
| F-42 | **TEST — welcome screen background colour** — *throwaway story, only to exercise the backlog → issue → Claude → PR → preview → merge workflow end to end.* Change the background of the welcome screen (the first screen, `#welcomeScreen`) to a warm sunset gradient instead of the current dark blue, keeping all text clearly readable. CSS only, in `src/styles/app.css` — no other screen, no JS, no new files. Delete this story once the workflow has been validated. *(test)* Shipped: `#welcomeScreen` now has a warm sunset gradient (deep indigo → plum → burnt coral/orange) replacing the dark-blue/starfield background; all foreground text/buttons keep sufficient contrast. Follow-up (issue #16): gradient expanded into a fuller 8-stop rainbow (indigo → violet → magenta → red → orange → gold → green → teal), all stops kept dark/saturated enough for white text to stay readable. | S | Low | — | Done |
| F-33 | **Hero character — video-game look** — a much more appealing main character: a 3D-looking SVG persona (depth, shading, gradients — the polish of a real kids' game, not flat clipart), with a few poses/expressions (idle, cheering, thinking, sad-but-kind). Becomes the face of the app: welcome screen, profiles, quiz sidekick. Art direction decision first (who is it — an animal? a kid? the unicorn?), then the SVG set. Supersedes the vague "funny pass" idea; F-07 is then just its reactions. *(new — user request)* | L | High | — | Todo |
| F-34 | **UX overhaul across every screen** — the whole app should look deliberately designed, not assembled: consistent spacing/typography scale, bigger tap targets for small hands, clearer hierarchy on the wheel / quiz / progress / profile screens, smoother transitions between them. A pass over `src/styles/app.css` with a written mini style guide so later work stays consistent. *(new — user request)* | L | High | — | Todo |
| F-36 | **Reward animations on every success** — something visibly fun each time she gets one right: funny animals dropping down the screen, confetti bursts, random little surprises, so success always feels celebrated. Must stay fast (never block the next question) and respect `prefers-reduced-motion`. Absorbs the "visual" half of F-05. *(new — user request)* | M | High | — | Todo |
| F-35 | **Merveilleux, bigger and wilder** — in Mode Merveilleux the unicorn should be much bigger and animated all around the screen (galloping across, sparkle trails, rainbow sweeps), not a small corner companion. The mode should feel like a different, magical place. *(new — user request)* | M | Med | F-31 | Todo |
| F-05 | **Celebration moments** — confetti + sound on streaks / perfect round. Cheap, big joy. *(visual half now covered by F-36, sound half by F-29)* | S | Med | F-29, F-36 | Todo |
| F-07 | **Mascot reactions** — the character reacts in-game (cheers, "essaie encore", thinks while you choose). Uses the persona from F-33. *(my idea D-b)* | M | Med | F-33 | Todo |
| F-09 | **Theme illustrations** — custom picture per theme on the wheel wedge + category badge. | M | Med | user art | Todo |
| F-13 | **Voice answering** — child speaks the answer; speech recognition interprets & checks it. Hands-free with F-01, and the natural pair to read-aloud for a pre-reader. *(idea #4)* | L | Med | T-09, T-10 | Todo |
| F-29 | **Funny sound & music** — playful kid audio for key moments: correct answer, wrong answer, level/milestone reached, win, encouragement; plus a mute toggle. Absorbs the "sound" half of F-05. **Scope (2026-07):** shipped as **SFX-only** (no looping background music — parent-friendly) with **6 moment sounds** (`spin`, `land`, `correct`, `wrong`, `win`, `cheer`) + one 🔊/🔇 mute toggle (persisted). Sounds are **real files on the Worker CDN** (`/audio/<name>.mp3`, mirrors image hosting T-23), fail-silent when muted/offline. Client code done in `src/audio.js`; **pending: source + upload the 6 CC0 files to the Worker** — until then the game is silent. | M | High | — | In progress |
| F-01 | **Read-aloud (TTS)** — voice reads the question (and options) aloud for pre-readers, auto or 🔊 button. Browser `speechSynthesis`, no backend. *(idea #3)* | M | Med | — | Done |

## 2. Game play

| ID | Item — what & why | Size | Priority | Depends on | Status |
|----|-------------------|:----:|:--------:|-----------|:------:|
| F-37 | **Merveilleux is consumed when played** — once she enters Mode Merveilleux after a 10-streak, the unlock is spent: leaving the mode locks it again and she must earn another 10 in a row. Keeps it a reward rather than a permanent shortcut (the secret parent shortcut stays, for showing it off). *(new — user request)* | S | Med | F-31 | Todo |
| F-10 | **Player profile + progress** — per-theme best scores, streaks, history that persists. Profiles + durable logging shipped; **progress page** (per-theme answered / accuracy / bonnes) and **live in-game score** under the player name shipped. *(idea #2, absorbs old F-06)* | M | Med | T-05 | In progress |
| F-11 | **Rewards, stickers & avatar** — collectibles/avatar earned by playing; return-driver for a 5-year-old. *(my idea D-c)* | M | Med | F-10 | Todo |
| F-15 | **Session modes** — ~~beyond 3/spin~~: the loop is now an **endless run per theme with a milestone every 10** (continue / back to wheel), replacing the fixed 3-per-spin. Timed "défi" mode still open. | M | Low | — | In progress |
| F-16 | **Leaderboard** — compare scores across players. *(idea #2)* **Scope (2026-09):** a "🏆 Classement" screen, entered via a button next to "📊 Mes progrès" on the wheel screen, ranking the sibling profiles under the signed-in account by lifetime correct answers (accuracy % as tie-breaker). RLS already scopes `profiles`/`attempts` to `owner = auth.uid()`, so this is same-account siblings only, never a cross-family leaderboard — no schema change needed. Shipped. | M | Low | F-10, T-05 | Done |
| F-17 | **Multiplayer across parallel sessions** — two or more children play **the same quiz at the same time on their own devices**, seeing each other's progress, with a shared result at the end. Async (compare later) is the cheap first step; live head-to-head is the real goal. **Explicitly the end goal — do it last**, once content, look and accounts are solid. *(idea #8, user: "this one for the end")* | XL | Low | F-16, T-03, T-05 | Todo |
| F-14 | **Advanced (open-ended) mode** — pick a theme → harder question with no multiple-choice options. *(idea #7)* | M | Low | F-20, T-09 | Todo |
| F-19 | **Surprise theme special behavior** — keep "random theme" or make it silly/jokes/mini-game. *(superseded by F-32 — Surprise becomes a mix of all themes)* | M | Low | — | Todo |
| F-30 | **"Révise tes erreurs" (replay failed)** — from the progress page, replay per-theme the questions whose most recent attempt was wrong. Shipped. *(new — user request)* | M | Med | F-10 | Done |
| F-31 | **"Mode Merveilleux" (streak unlock)** — a special mode that **unlocks after 10 correct answers in a row** (session-wide, in memory only — lost on reload/sign-out, no DB change); a reward for sustained focus. A wrong answer resets the streak; "Révise tes erreurs" replays don't count. Unlock triggers a confetti popup ("Jouer maintenant" / "Plus tard") and reveals a glowing 🦄 button next to the wheel (not a wheel wedge, so the 7-slice wheel and Surprise mix are untouched). Serves a new `merveilleux` theme: 50 whimsical questions (licornes, anniversaires, fêtes, gâteaux, fées, arc-en-ciel, dragons gentils…) in `questions.js`, with a rainbow/pastel/sparkle CSS skin (`src/styles/app.css`) — text-only, no DB reseed needed (falls back to `window.QUIZ_DATA`). Dev shortcut `?streak=9` for testing. Shipped. **Follow-up (2026-09):** the unlock/entry-button/question-card unicorn is now a real inline SVG (gradient rainbow mane, glowing horn, twinkling sparkles) instead of emoji, with gentle motion respecting `prefers-reduced-motion`; the streak + unlock now also reset on sign-in and on switching player profile (previously only sign-out), so siblings sharing a device never inherit each other's progress. Shipped. **Follow-up (2026-09):** a secret parent/dev shortcut — typing `licorne` (or `Ctrl+Shift+M`) on desktop, 5 quick taps on the wheel screen title on touch — unlocks and jumps straight into Mode Merveilleux without earning the streak, for showing it off or testing. Silent (no UI hint), never fires while typing in a form field, session-only like the real unlock (resets on sign-out/in, profile switch, reload); `?streak=9` still works too. Shipped. *(new — user request)* | M | Med | F-10 | Done |
| F-32 | **Surprise = mix of all themes** — the Surprise wheel segment now draws its pool by combining the 6 other themes (Maths, Lecture, Géographie, Astronomie, Animaux, Arabe) instead of its own dedicated bank. Each question card shows a small themed tag (emoji + label) naming which theme it actually came from. Shipped. *(new — user request)* | S | Med | — | Done |
| F-21 | **7-theme spinning wheel** — Maths, Lecture, Géographie, Astronomie, Animaux, Arabe, Surprise; one wedge each. | — | — | — | Done |
| F-22 | **Click a wheel slice to select a theme** — direct pick, no spin. | — | — | — | Done |
| F-23 | **3 questions per spin + score screen** — end-of-session score with emoji feedback. | — | — | — | Done |

## 3. Curriculum

| ID | Item — what & why | Size | Priority | Depends on | Status |
|----|-------------------|:----:|:--------:|-----------|:------:|
| F-43 | **Change the Arabic Curriculum** — Replace the current set of Arabic question with questions going over the alphabet in Arabic - and making the player guess the first letter based on a name + picture. E.g. show a rabbit (Arnab) and have the player guess whether it is alif, ba, ta or tha. Question and sound has to be in Arabic. Old question can be kept as back up. Shipped: `arabe` theme in `questions.js` replaced with 81 alphabet questions covering all 28 letters (28 facile: word/emoji → first letter; 28 moyen: same words, visually-confusable distractors; 25 difficile: letter → word reverse-lookup + alphabet-order facts). Each question shows an emoji + the Arabic word (no new image assets, since media can't be uploaded to the CDN from here) and, when read aloud, the embedded Arabic word/letter is spoken with an Arabic voice/lang if the browser has one (`src/tts.js`, `src/quiz.js`). Old 100-question vocab/greetings set kept as `arabeClassique` in `questions.js` (inert backup, not on the wheel). **Refinement (#20):** the French instruction phrase and Latin transliteration glosses were dropped from all 81 questions/options — the visible text is now fully Arabic (just emoji + Arabic word/letter), and read-aloud now speaks the whole question + options with the Arabic voice/lang instead of only the embedded word. The question card and choices also switch to `dir="rtl"` for Arabe-origin questions. Needs you: reseed the Supabase `questions` table (`tools/migrate-questions-to-db.js`, needs the service-role key) so production picks up the new content — until then the DB still serves the old set, since `questions.js` is only the offline fallback. | L | High | — | Done |
| F-39 | **Align content with the school programme (CP first)** — rewrite/extend each theme so it follows the official French **CP** programme (where she is now), then CE1, CE2… as she moves up. Per subject, list what CP covers (lecture: syllables, sons complexes…; maths: nombres jusqu'à 100, additions, doubles…) and make sure the bank covers it rather than scattering random facts. Grade becomes a property of a question (`level: CP`), so the app can serve "her class". *(new — user request)* | L | High | — | Todo |
| F-40 | **Difficulty levels within each class** — inside a grade (CP, CE1…), keep easy/medium/hard so the same class stays playable as she improves. Replaces the vague global 1/2/3 scale with grade + level, and makes F-02/F-03 concrete. *(new — user request)* | M | High | F-39 | Todo |
| F-38 | **Scale to ~1000 questions per category** — from ~100 today to about 1000 per theme, so the game never repeats itself for long. Realistically LLM-generated (F-20) with a parent review pass, not hand-authored; needs a generation + review + dedupe workflow. *(new — user request)* | XL | High | F-20, F-39 | Todo |
| F-41 | **Parents create questions from their own material** — upload a photo/PDF/text of what the class is doing (a worksheet, a lesson page), and the app turns it into questions for that child. Makes the game follow the actual classroom. Needs upload + extraction + LLM generation + a parent review/approve step before anything reaches the child. *(new — user request)* | L | Med | T-03, F-20 | Todo |
| F-20 | **Dynamic / endless questions** — questions (and images) generated on demand by an LLM instead of the fixed static list, so content feels fresh and never runs out. *(idea #1; engine/safety/cost work in T-07/T-08, image licensing T-13)* | L | High | T-03, T-07, T-08 | Todo |
| F-02 | **Difficulty UX decision** — sticky vs per-spin vs adaptive. Unblocks the difficulty feature. | S | High | — | Todo |
| F-03 | **Difficulty selector + filter** — pick a level; quiz draws only matching questions. Data already tagged. | M | High | F-02, F-40 | Todo |
| F-04 | **Fill facile & difficile banks** — most content is `moyen`; author level-1 & level-3 so difficulty is meaningful. *(may be served by LLM gen, F-20 / F-38)* | L | High | — | Todo |
| F-06 | **Parent dashboard** — a view for you: progress, frequent mistakes, set/lock difficulty & themes, and (with F-41) manage the questions you added. *(my idea D-a)* | M | Med | F-10 | Todo |
| F-08 | **Finish question images** — 118 / 910 referenced images don't exist yet; source or generate them. | M | Med | — | Todo |
| F-12 | **Adaptive difficulty** — auto-nudge level from recent performance. *(my idea D-d)* | M | Low | F-03 | Todo |
| F-18 | **Audio-prompt questions** — sound *is* the prompt (animal sounds, Arabic pronunciation). | L | Low | T-15 | Todo |
| F-27 | **Translate everything to English** — full i18n: language toggle + translated UI chrome + English question content. This first translation establishes the i18n plumbing (string catalog + per-language question sets). **Scope (scoped 2026-07):** two jobs — (1) *UI chrome* ~100–130 strings across `src/` modules + `index.html` + segments labels (mechanical, ~½ day); (2) *content* — the **700 questions** in `questions.js` (the bulk). Recommended = **bilingual toggle (keep FR, add EN)**, not a straight swap, since FR is the learning language. Plumbing: new `src/i18n.js` (`t()` + string catalog), a `lang` setting persisted per profile (`profiles.lang` column), a 🇫🇷/🇬🇧 toggle, and per-locale question fields (`question_en`/`options_en`/`answer_en`). **Gotchas:** `Lecture` (FR reading) must be **re-authored** as English reading, not translated; `Arabe` stays Arabic but reframed EN; images are language-neutral (reusable). Stage it: ship UI+toggle first, then translate content theme-by-theme (Lecture last). *(the ~700 questions are the bulk of the effort)* | L | Med | — | Todo |
| F-28 | **Translate everything to Arabic** — Arabic UI + question content, **plus right-to-left (RTL) layout**. Reuses F-27's i18n plumbing. (Distinct from the existing `Arabe` learning theme, which teaches Arabic letters/words within the French app.) | L | Med | F-27 | Todo |
| F-24 | **Text MCQ + image question formats** — illustration, image-as-question, 2×2 image-option grid. | — | — | — | Done |
| F-25 | **700 questions across 7 themes** — ≈175 facile / 350 moyen / 175 difficile. | — | — | — | Done |
| F-26 | **792 / 910 question images sourced** — French Wikipedia, graceful hide-on-missing. | — | — | — | Done |
